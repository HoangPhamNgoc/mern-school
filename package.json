{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run build"
  }
}
